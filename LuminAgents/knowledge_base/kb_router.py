# knowledge_base/kb_router.py
# v7.0 STUB — Local KB permanently bypassed (SKIP_LOCAL_KB=True / Web-First Protocol).
# Kept as import stub to prevent ImportError in researcher.py.
# All knowledge retrieval is handled by Tier 3 (Tavily) and Tier 4 (LLM Synthesis).

def search(query: str, tags: list = None, level: str = "beginner", limit: int = 3) -> list:
    """Stub — returns empty list. Web-First Protocol active."""
    return []
